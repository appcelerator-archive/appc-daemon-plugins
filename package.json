{
  "name": "appc-daemon-plugins",
  "version": "0.0.0",
  "private": true,
  "devDependencies": {
    "lerna": "^3.16.4"
  },
  "scripts": {
    "build": "lerna exec gulp build",
    "link": "lerna exec yarn link",
    "unlink": "lerna exec yarn unlink"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "dependencies": {
    "gulp": "^4.0.2",
    "node-gyp": "^5.0.3"
  }
}
